export type AccountItem = {
  id: string;
  userID: string;
  label: string;
  description: string;
  initValue: number;
  createdAt: string;
  updatedAt: string;
  total: number;
  type?: string;
};
